# Nothing specific to macOS & zsh currently but better to be explicit
# as there's no easy way to optionally load the profile easily without
# errors when it doesn't exist, but show errors within it. A log would
# help.
